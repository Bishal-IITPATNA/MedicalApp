# Core Flask dependencies (compatible versions)
Flask==3.0.0
Flask-SQLAlchemy==3.0.5
SQLAlchemy==2.0.21
Flask-JWT-Extended==4.5.3
Flask-CORS==4.0.0
Flask-Migrate==4.0.5

# Database (using SQLite for now, can upgrade to PostgreSQL later)
# psycopg2-binary==2.9.7  # Uncomment when using PostgreSQL

# Production server
gunicorn==21.2.0

# Additional production dependencies
python-dotenv==1.0.0
requests==2.31.0

# SMS Service (optional)
twilio==8.10.0

# Azure SDK (optional)
azure-storage-blob==12.19.0
azure-identity==1.15.0
